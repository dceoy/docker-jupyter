version: '3'

services:
  jupyter:
    container_name: jupyter
    build:
      context: .
    image: dceoy/jupyter:latest
    user: ${UID}:${GID}
    ports:
      - 8888:8888
    volumes:
      - ${HOME}:/work
    working_dir: /work
